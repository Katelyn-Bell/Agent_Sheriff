import { PageHeader } from "@/components/PageHeader";

export default function LedgerPage() {
  return (
    <section>
      <PageHeader
        title="Sheriff's Ledger"
        subtitle="Audit history · filters · row detail"
      />
      <p className="text-ink-soft">Audit workbench coming in Run 3C.</p>
    </section>
  );
}
