import { PageHeader } from "@/components/PageHeader";

export default function TrialRecordsPage() {
  return (
    <section>
      <PageHeader
        title="Trial Records"
        subtitle="Replay evaluation runs"
      />
      <p className="text-ink-soft">Eval runs coming in Run 4A.</p>
    </section>
  );
}
